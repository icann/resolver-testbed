---
- name: Install generic packages
  ansible.builtin.apt:
    pkg:
      - wget
      - vim
      - tmux
      - lsb-release
      - ca-certificates
      - python3-pip
      - apt-transport-https
    state: present
    update_cache: yes
