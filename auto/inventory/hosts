all:
  hosts:
    VBox-host
  children:
    vms:
      # Make sure to tidy the vagrant environment (destroy boxes prior of their
      # removal here) before changing values here.
      resolvers-vm:
      servers-vm:
      gateway-vm:
